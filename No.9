import pandas as pd

# Sample DataFrame
data = {
    "Name": ["Alice", "Bob", "Charlie", "David"],
    "Age": [25, 30, 35, 40],
    "Score": [85, 90, 95, 88]
}
df = pd.DataFrame(data)

# Using .loc to select rows and columns
# Example 1: Select the row with index 1 (Bob)
print(df.loc[1])

# Example 2: Select "Age" column for rows 0 and 2
print(df.loc[[0, 2], "Age"])

# Example 3: Select rows where Age > 30
print(df.loc[df["Age"] > 30]
divmod(7,3)

seconds = 367
minutes, seconds_left = divmod(seconds, 60)

print(f"{minutes} دقیقه و {seconds_left} ثانیه")
#about iloc if you  have a list and want to change it for example if the name of person be Alice 
df.loc['Alice'] =[75 , 'clerck']
#if the first person be Alice by df.iloc[0 you can get every thing about her you can even devide numbers by df.age*2 or other stuff

